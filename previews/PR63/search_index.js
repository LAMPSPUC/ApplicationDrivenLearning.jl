var documenterSearchIndex = {"docs":
[{"category":"section","location":"examples/newsvendor/#Newsvendor-Problem","page":"Newsvendor Problem","text":"This example shows how to use the ApplicationDrivenLearning.jl package to solve a newsvendor problem.\n\nThe newsvendor problem is a classic inventory management problem where a company must decide how many units to produce to meet demand. The company wants to minimize the cost of holding excess inventory while also minimizing the cost of lost sales. ","title":"Newsvendor Problem"},{"category":"section","location":"examples/newsvendor/#The-model","page":"Newsvendor Problem","text":"For this problem, both plan and assess models are defined as:\n\nmin cx - qy - rw \ntextst quad xyw geq 0 \ny + w leq x \ny leq d\n\nThe plan model will be run considering the demand d equal to the output of the forecast model, defining the policy x that will be used in the assess model. The assess model will be run considering the demand d equal to the actual demand.","title":"The model"},{"category":"section","location":"examples/newsvendor/#Data","page":"Newsvendor Problem","text":"We will model two separate items. One of the items presents a low overstocking cost and the other presents a low understocking cost, generating different incentives that a decision maker could explore. This is achieved manipulating the costs:\n\ni=1 longrightarrow c=10 quad q = 19quad r = 9\n\ni=2 longrightarrow c=10 quad q = 11quad r = 1\n\nThe demand series for both items will be generated using a discrete uniform distribution.\n\nLet's start by loading the necessary packages and defining the problem parameters.\n\nusing Flux\nusing JuMP\nusing Random\nusing Gurobi\nusing ApplicationDrivenLearning\n\nADL = ApplicationDrivenLearning\n\n# data\nRandom.seed!(123)\nc = [10, 10]\nq = [19, 11]\nr = [9, 1]\n\nNow, we can initialize the application driven learning model, build the plan and assess models and set the forecast model.\n\n# init application driven learning model\nmodel = ADL.Model()\n@variables(model, begin\n    x[i=1:2] >= 0, ADL.Policy\n    d[i=1:2], ADL.Forecast\nend)\n\n# plan model\n@variables(Plan(model), begin\n    y_plan[i=1:2] >= 0\n    w_plan[i=1:2] >= 0\nend)\n@constraints(Plan(model), begin\n    [i=1:2], y_plan[i] + w_plan[i] <= x[i].plan\n    [i=1:2], y_plan[i] <= d[i].plan\nend)\n@objective(Plan(model), Min, sum(c[i] * x[i].plan - q[i] * y_plan[i] - r[i] * w_plan[i] for i in 1:2))\n\n# assess model\n@variables(Assess(model), begin\n    y_assess[i=1:2] >= 0\n    w_assess[i=1:2] >= 0\nend)\n@constraints(Assess(model), begin\n    [i=1:2], y_assess[i] + w_assess[i] <= x[i].assess\n    [i=1:2], y_assess[i] <= d[i].assess\nend)\n@objective(Assess(model), Min, sum(c[i] * x[i].assess - q[i] * y_assess[i] - r[i] * w_assess[i] for i in 1:2))\n\nset_optimizer(model, Gurobi.Optimizer)\nset_silent(model)\n\n# forecast model: one architecture producing both demands\nADL.set_forecast_model(\n    model,\n    ADL.ForecastModel(\n        architecture = Flux.Dense(1 => 2, exp),\n        outputs = [d[1], d[2]],\n    ),\n)\n\nThen, we can initialize the data, referencing forecast variables.\n\nx_d = ones(100, 1) .|> Float32\ny_d = Dict(\n    d[1] => rand(10:100, 100) .|> Float32,\n    d[2] => rand(10:100, 100) .|> Float32\n)\n\nWe can check how the model performs by computing the assess cost with the initial (random) forecast model.\n\njulia> ADL.compute_cost(model, x_d, y_d)\n-3.571615f0\n\nNow let's train the model using the GradientMode.\n\njulia> gd_sol = ApplicationDrivenLearning.train!(\n    model, x_d, y_d,\n    ApplicationDrivenLearning.Options(\n        ApplicationDrivenLearning.GradientMode,\n        rule=Flux.Adam(0.1),\n        epochs=30\n    )\n)\nEpoch 1 | Time = 0.4s | Cost = -3.57\nEpoch 2 | Time = 0.8s | Cost = -4.36\nEpoch 3 | Time = 1.2s | Cost = -5.33\nEpoch 4 | Time = 1.6s | Cost = -6.51\nEpoch 5 | Time = 2.0s | Cost = -7.95\nEpoch 6 | Time = 2.4s | Cost = -9.72\nEpoch 7 | Time = 2.8s | Cost = -11.88\nEpoch 8 | Time = 3.2s | Cost = -14.53\nEpoch 9 | Time = 3.6s | Cost = -17.78\nEpoch 10 | Time = 4.0s | Cost = -21.77\nEpoch 11 | Time = 4.4s | Cost = -26.68\nEpoch 12 | Time = 4.8s | Cost = -32.73\nEpoch 13 | Time = 5.2s | Cost = -39.52\nEpoch 14 | Time = 5.6s | Cost = -46.64\nEpoch 15 | Time = 6.0s | Cost = -54.15\nEpoch 16 | Time = 6.4s | Cost = -62.95\nEpoch 17 | Time = 6.8s | Cost = -74.74\nEpoch 18 | Time = 7.2s | Cost = -90.36\nEpoch 19 | Time = 7.6s | Cost = -110.35\nEpoch 20 | Time = 8.0s | Cost = -135.12\nEpoch 21 | Time = 8.4s | Cost = -164.34\nEpoch 22 | Time = 8.8s | Cost = -197.82\nEpoch 23 | Time = 9.2s | Cost = -237.1\nEpoch 24 | Time = 9.6s | Cost = -282.57\nEpoch 25 | Time = 10.0s | Cost = -334.87\nEpoch 26 | Time = 10.4s | Cost = -389.66\nEpoch 27 | Time = 10.8s | Cost = -442.7\nEpoch 28 | Time = 11.2s | Cost = -469.92\nEpoch 29 | Time = 11.6s | Cost = -452.58\nEpoch 30 | Time = 12.0s | Cost = -430.85\nApplicationDrivenLearning.Solution(-469.91516f0, Real[1.6040976f0, 1.2566354f0, 2.8811285f0, 1.1966338f0])\n\njulia> ADL.compute_cost(model, x_d, y_d)\n-469.91516f0\n\nAfter training, we can check the cost of the solution found by the gradient mode and even analyze the predictions from it.\n\njulia> model.forecast(x_d[1,:])\n2-element Vector{Float32}:\n 88.69701\n 11.626293\n\nAs we can see, the forecast model overestimates the demand for the first item and underestimates the demand for the second item (both items average demand is 55), following the incentives from the model structure.","title":"Data"},{"category":"section","location":"tutorials/modes/#Training-modes","page":"Training modes","text":"Training a model with ApplicationDrivenLearning is done by calling the train! function. This function takes an Options object whose first argument is the mode, i.e. the algorithm used to train the model; the remaining keyword arguments configure that algorithm. In this tutorial, we will show how to use the different modes and explore the trade-off between them.\n\nThe data used in this tutorial is the same as in the getting started tutorial so all the code shown in this page assumes that the full model (ApplicationDrivenLearning.Model) is already defined.","title":"Training modes"},{"category":"section","location":"tutorials/modes/#Bilevel-mode","page":"Training modes","text":"The bilevel mode mounts the bilevel optimization problem relative to training the predictive model and uses the BilevelJuMP package to solve it. For using this mode, we have to install the BilevelJuMP package, import it and set the BilevelJuMP mode (from BilevelJuMP.jl docs).","title":"Bilevel mode"},{"category":"section","location":"tutorials/modes/#Arguments","page":"Training modes","text":"optimizer: JuMP.jl optimizer used to solve the bilevel model using BilevelJuMP.jl.\nmode: The mode to use for the bilevel optimization problem. It can be any of the modes supported by the BilevelJuMP package.\nsilent: When true, suppresses the solver output of the bilevel optimization problem.","title":"Arguments"},{"category":"section","location":"tutorials/modes/#Example","page":"Training modes","text":"julia> import BilevelJuMP\njulia> import HiGHS\n\njulia> opt = ApplicationDrivenLearning.Options(\n    ApplicationDrivenLearning.BilevelMode,\n    optimizer = HiGHS.Optimizer,\n    mode = BilevelJuMP.FortunyAmatMcCarlMode(\n        primal_big_M = 100,\n        dual_big_M = 100,\n    )\n);\njulia> sol = ApplicationDrivenLearning.train!(model, X, Y, opt)\nRunning HiGHS 1.9.0 (git hash: 66f735e60): Copyright (c) 2024 HiGHS under MIT licence terms\nCoefficient ranges:\n  Matrix [1e+00, 2e+04]\n  Cost   [5e-01, 5e+00]\n  Bound  [1e+00, 1e+00]\n  RHS    [5e-01, 1e+02]\nAssessing feasibility of MIP using primal feasibility and integrality tolerance of       1e-06\nWARNING: Row      0 has         infeasibility of          10 from [lower, value, upper] = \n[             10;               0;              10]\nSolution has               num          max          sum\nCol     infeasibilities      0            0            0\nInteger infeasibilities      0            0            0\nRow     infeasibilities      8           20           42\nRow     residuals            0            0            0\nAttempting to find feasible solution by solving LP for user-supplied values of discrete variables\nCoefficient ranges:\n  Matrix [1e+00, 2e+04]\n  Cost   [5e-01, 5e+00]\n  Bound  [0e+00, 0e+00]\n  RHS    [5e-01, 1e+02]\nPresolving model\n12 rows, 7 cols, 24 nonzeros  0s\n6 rows, 7 cols, 12 nonzeros  0s\n4 rows, 5 cols, 8 nonzeros  0s\n4 rows, 5 cols, 8 nonzeros  0s\nPresolve : Reductions: rows 4(-32); columns 5(-28); elements 8(-80)\nSolving the presolved LP\nUsing EKK dual simplex solver - serial\n  Iteration        Objective     Infeasibilities num(sum)\n          0    -3.1250000000e+02 Ph1: 4(5623); Du: 1(0.3125) 0s\n          3     3.0000000000e+02 Pr: 0(0) 0s\nSolving the original LP from the solution after postsolve\nModel status        : Optimal\nSimplex   iterations: 3\nObjective value     :  3.0000000000e+02\nRelative P-D gap    :  0.0000000000e+00\nHiGHS run time      :          0.05\nPresolving model\n24 rows, 17 cols, 52 nonzeros  0s\n17 rows, 14 cols, 40 nonzeros  0s\n10 rows, 11 cols, 20 nonzeros  0s\n4 rows, 5 cols, 8 nonzeros  0s\n\nMIP start solution is feasible, objective value is 300\n\nSolving MIP model with:\n   4 rows\n   5 cols (0 binary, 0 integer, 0 implied int., 5 continuous)\n   8 nonzeros\n\nSrc: B => Branching; C => Central rounding; F => Feasibility pump; H => Heuristic; L => Sub-MIP;\n     P => Empty MIP; R => Randomized rounding; S => Solve LP; T => Evaluate node; U => Unbounded;\n     z => Trivial zero; l => Trivial lower; u => Trivial upper; p => Trivial point        \n\n        Nodes      |    B&B Tree     |            Objective Bounds              |  Dynamic Constraints |       Work\nSrc  Proc. InQueue |  Leaves   Expl. | BestBound       BestSol              Gap |   Cuts  \n InLp Confl. | LpIters     Time\n\n         0       0         0   0.00%   -inf            300                Large        0  \n    0      0         0     0.0s\n         1       0         1 100.00%   300             300                0.00%        0  \n    0      0         4     0.1s\n\nSolving report\n  Status            Optimal\n  Primal bound      300\n  Dual bound        300\n  Gap               0% (tolerance: 0.01%)\n  P-D integral      0\n  Solution status   feasible\n                    300 (objective)\n                    0 (bound viol.)\n                    0 (int. viol.)\n                    0 (row viol.)\n  Timing            0.11 (total)\n                    0.00 (presolve)\n                    0.00 (solve)\n                    0.00 (postsolve)\n  Max sub-MIP depth 0\n  Nodes             1\n  Repair LPs        0 (0 feasible; 0 iterations)\n  LP iterations     4 (total)\n                    0 (strong br.)\n                    0 (separation)\n                    0 (heuristics)\nApplicationDrivenLearning.Solution(300.0, Real[20.0f0])\n\nThe output shows the solution found by the optimizer. The first value is the cost of the solution and the second value is the predictive model parameters.","title":"Example"},{"category":"section","location":"tutorials/modes/#Pros","page":"Training modes","text":"Is the most accurate option as it represents the bilevel optimization problem exactly.","title":"Pros"},{"category":"section","location":"tutorials/modes/#Cons","page":"Training modes","text":"Requires solving a MIP problem that grows in size very fast with the number of data points.\nRelies on parameters specific to the BilevelJuMP package.\nDoes not support integer variables in the plan and assess models.","title":"Cons"},{"category":"section","location":"tutorials/modes/#Optim-mode","page":"Training modes","text":"Uses any algorithm from the Optim.jl package, which is already a dependency, so nothing extra needs installing or loading.\n\nThe objective handed to Optim is the assessed cost as a function of the flat parameter vector of the predictive model. Derivative-free algorithms need nothing further. Gradient-based ones are additionally given dC/dθ — the same reverse pass the gradient mode uses, carried one step further from the forecasts to the parameters.","title":"Optim mode"},{"category":"section","location":"tutorials/modes/#Arguments-2","page":"Training modes","text":"algorithm: the Optim.jl algorithm object. Defaults to Optim.NelderMead(). Derivative-free choices include Optim.NelderMead(), Optim.ParticleSwarm() and Optim.SimulatedAnnealing(); gradient-based ones include Optim.LBFGS(), Optim.BFGS(), Optim.ConjugateGradient() and Optim.GradientDescent().\nlower_bounds, upper_bounds: element-wise bounds on the parameters. When either is given the search is run box-constrained, with the algorithm wrapped in Optim.Fminbox. Be aware that Fminbox runs its inner algorithm to convergence on every outer iteration, so it is considerably more expensive.\nAny other parameter acceptable on Optim.Options such as iterations, time_limit and g_abstol can be directly passed.\nparallel: the backend used for the per-sample cost and gradient evaluations. Defaults to SerialBackend(); MPIBackend() distributes them across MPI processes and DistributedBackend() across Distributed worker processes. See running in parallel.","title":"Arguments"},{"category":"section","location":"tutorials/modes/#Example-2","page":"Training modes","text":"opt = ApplicationDrivenLearning.Options(\n    ApplicationDrivenLearning.OptimMode,\n    algorithm = Optim.LBFGS(),\n    iterations = 200,\n)\nsol = ApplicationDrivenLearning.train!(model, X, Y, opt)","title":"Example"},{"category":"section","location":"tutorials/modes/#Choosing-between-them","page":"Training modes","text":"The assessed cost of a linear application is piecewise linear in the forecasts, so dC/dθ is piecewise constant with kinks wherever the optimal active set changes. On a problem that is convex in the parameters every algorithm here reaches the same optimum; on one that is not, different algorithms legitimately settle on different local optima, and gradient-based methods may stall at a kink. The derivative-free default is the safer starting point.\n\nA related trap: nothing stops an optimizer proposing parameters whose predictions the application cannot accommodate — a negative forecast where the plan model needs a non-negative one, leaving the plan model infeasible. lower_bounds and upper_bounds are the remedy, and the error raised in that situation says so.","title":"Choosing between them"},{"category":"section","location":"tutorials/modes/#Nelder-Mead-mode","page":"Training modes","text":"compat: Deprecated\nSuperseded by Optim mode, which reaches every Optim.jl algorithm rather than just this one. It behaves exactly as before and existing code keeps working, but note that initial_simplex and parameters are Nelder-Mead specific, so under OptimMode they move into the algorithm object: Optim.NelderMead(initial_simplex = ...).\n\nUses the Nelder-Mead algorithm implemented in the Optim.jl package. This algorithm is a gradient-free optimization algorithm that does not require the gradient of the objective function and is very robust to the choice of the initial guess.","title":"Nelder-Mead mode"},{"category":"section","location":"tutorials/modes/#Arguments-3","page":"Training modes","text":"initial_simplex: A n + 1 dimensional vector of n-dimensional vectors, where n is\n\nthe dimension of predictive model parameters. It will be used to start the search algorithm.\n\nparameters: Used to generate parameters for the algorithm. Same parameter from\n\nOptim implementation of Nelder-Mead.\n\nAny other parameter acceptable on Optim.Options such as iterations, time_limit\n\nand g_abstol can be directly passed.","title":"Arguments"},{"category":"section","location":"tutorials/modes/#Example-3","page":"Training modes","text":"julia> opt = ApplicationDrivenLearning.Options(\n    ApplicationDrivenLearning.NelderMeadMode,\n    show_trace = true\n);\njulia> sol = ApplicationDrivenLearning.train!(model, X, Y, opt)\nIter     Function value    √(Σ(yᵢ-ȳ)²)/n\n------   --------------    --------------\n     0     1.473289e+03     5.201782e+00\n * time: 0.0009999275207519531\n     1     1.473289e+03     1.560535e+01\n * time: 0.0019998550415039062\n     2     1.442079e+03     4.681610e+01\n * time: 0.003000020980834961\n     3     1.348446e+03     1.404482e+02\n * time: 0.004999876022338867\n     4     1.067550e+03     2.962982e+02\n * time: 0.006000041961669922\n     5     4.749537e+02     3.450557e+01\n * time: 0.007999897003173828\n     6     4.059426e+02     3.511205e+01\n * time: 0.009999990463256836\n     7     3.357185e+02     6.064606e-01\n * time: 0.013000011444091797\n     8     3.345056e+02     8.778015e+00\n * time: 0.018999814987182617\n     9     3.169496e+02     8.171539e+00\n * time: 0.020999908447265625\n    10     3.006065e+02     1.588013e+00\n * time: 0.021999835968017578\n    11     3.006065e+02     5.784607e-02\n * time: 0.023999929428100586\n    12     3.004908e+02     1.371613e-01\n * time: 0.026000022888183594\n    13     3.002165e+02     7.929993e-02\n * time: 0.029999971389770508\n    14     3.000579e+02     2.355957e-02\n * time: 0.03399991989135742\n    15     3.000107e+02     2.166748e-03\n * time: 0.03600001335144043\n    16     3.000064e+02     2.151543e-03\n * time: 0.03699994087219238\n    17     3.000021e+02     5.342755e-04\n * time: 0.03900003433227539\n    18     3.000010e+02     4.882812e-04\n * time: 0.039999961853027344\n    19     3.000001e+02     9.155273e-05\n * time: 0.04199981689453125\n    20     3.000001e+02     3.051758e-05\n * time: 0.046000003814697266\n    21     3.000000e+02     0.000000e+00\n * time: 0.04999995231628418\nApplicationDrivenLearning.Solution(300.0f0, Real[20.0f0])","title":"Example"},{"category":"section","location":"tutorials/modes/#Pros-2","page":"Training modes","text":"Is the most general option as it can be used with basically any plan, assess and forecast model.\nDoes not require the gradient of the objective function.","title":"Pros"},{"category":"section","location":"tutorials/modes/#Cons-2","page":"Training modes","text":"Relies on a lot of iterations to accurately optimize the model parameters.\nDepends on the initial guess that can be given by the user or generated randomly by the Optim.jl package.","title":"Cons"},{"category":"section","location":"tutorials/modes/#NLopt-mode","page":"Training modes","text":"Uses an algorithm from the NLopt.jl package, through a package extension.\n\nnote: Note\nNLopt is a weak dependency: install it and add using NLopt before training with this mode. The mode type itself always exists, so forgetting the using gives a readable error rather than an UndefVarError.","title":"NLopt mode"},{"category":"section","location":"tutorials/modes/#Arguments-4","page":"Training modes","text":"algorithm::Symbol: the NLopt algorithm. Defaults to :LN_NELDERMEAD. NLopt encodes the kind in the name: :LN_* are local derivative-free (:LN_BOBYQA, :LN_COBYLA), :GN_* global derivative-free (:GN_DIRECT), and :LD_* gradient-based (:LD_LBFGS, :LD_MMA, :LD_SLSQP). The gradient-based ones are given dC/dθ automatically.\nAny other parameter is set on the NLopt Opt object, so stopping criteria use NLopt's names — xtol_rel, xtol_abs, ftol_rel, ftol_abs, maxeval, maxtime, stopval — and bounds are lower_bounds and upper_bounds. This differs from Optim mode, where such keywords are forwarded to Optim.Options instead.\nparallel: the backend used for the per-sample cost and gradient evaluations. Defaults to SerialBackend(); MPIBackend() distributes them across MPI processes and DistributedBackend() across Distributed worker processes. See running in parallel.","title":"Arguments"},{"category":"section","location":"tutorials/modes/#Example-4","page":"Training modes","text":"using NLopt\n\nopt = ApplicationDrivenLearning.Options(\n    ApplicationDrivenLearning.NLoptMode,\n    algorithm = :LD_LBFGS,\n    lower_bounds = [0.0],\n    xtol_rel = 1e-8,\n    maxeval = 2000,\n)\nsol = ApplicationDrivenLearning.train!(model, X, Y, opt)","title":"Example"},{"category":"section","location":"tutorials/modes/#Gradient-mode","page":"Training modes","text":"By computing the gradient of the assessed cost with respect to the forecast values, this mode propagates the gradient to the predictive model parameters, guiding the parameter update process. Since it uses the model structure end-to-end to guide training, it typically requires fewer iterations to achieve good results. However, it may suffer from known issues of gradient-based optimization methods, such as sensitivity to learning rate selection.\n\nThe gradient algorithm runs for a specified number of iterations, saving the best available parameter values until the end or convergence.","title":"Gradient mode"},{"category":"section","location":"tutorials/modes/#Arguments-5","page":"Training modes","text":"rule: The optimizer for the gradient algorithm. Has to be an instance of optimization\n\nrules from Flux.jl.\n\nepochs: The number of iterations to run the gradient algorithm.\nbatch_size: The batch size to use for the gradient algorithm. If -1, the entire dataset is used. When positive, each epoch draws batch_size sample indexes uniformly with replacement, so a batch may repeat samples and an epoch does not sweep the whole dataset.\nverbose: Whether to print the progress of the gradient algorithm.\ncompute_cost_every: Allows for cost computation of every sample to be run only\n\nafter a specified number of epochs. This enables faster iterations with the drawback of possibly missing sets of parameters with low associated cost.\n\ntime_limit: The time limit for the gradient algorithm in seconds.\ng_tol: Convergence condition on the infinity norm of the per-sample cost gradients with respect to the forecasts (the maximum absolute entry over all samples and outputs). Training stops as soon as this norm falls below g_tol. The default is 0, which effectively disables the check.\nparallel: the backend used for the per-sample cost and gradient evaluations. Defaults to SerialBackend(); MPIBackend() distributes them across MPI processes and DistributedBackend() across Distributed worker processes. See running in parallel.","title":"Arguments"},{"category":"section","location":"tutorials/modes/#Example-5","page":"Training modes","text":"Below, we illustrate the use of GradientMode to optimize the predictive model used in the ongoing example.\n\njulia> opt = ApplicationDrivenLearning.Options(\n    ApplicationDrivenLearning.GradientMode,\n    rule = Flux.Descent(0.01),\n);\njulia> sol = ApplicationDrivenLearning.train!(model, X, Y, opt)\nEpoch 1 | Time = 0.0s | Cost = 1411.12\nEpoch 2 | Time = 0.0s | Cost = 1330.12\nEpoch 3 | Time = 0.1s | Cost = 1249.12\nEpoch 4 | Time = 0.1s | Cost = 1168.12\nEpoch 5 | Time = 0.1s | Cost = 1087.12\nEpoch 6 | Time = 0.1s | Cost = 1006.12\nEpoch 7 | Time = 0.2s | Cost = 925.12\nEpoch 8 | Time = 0.2s | Cost = 844.12\nEpoch 9 | Time = 0.2s | Cost = 763.12\nEpoch 10 | Time = 0.2s | Cost = 682.12\nEpoch 11 | Time = 0.2s | Cost = 601.12\nEpoch 12 | Time = 0.3s | Cost = 573.37\nEpoch 13 | Time = 0.3s | Cost = 564.37\nEpoch 14 | Time = 0.3s | Cost = 555.37\nEpoch 15 | Time = 0.3s | Cost = 546.37\nEpoch 16 | Time = 0.4s | Cost = 537.37\nEpoch 17 | Time = 0.4s | Cost = 528.37\nEpoch 18 | Time = 0.4s | Cost = 519.37\nEpoch 19 | Time = 0.4s | Cost = 510.37\nEpoch 20 | Time = 0.5s | Cost = 501.37\nEpoch 21 | Time = 0.5s | Cost = 492.37\nEpoch 22 | Time = 0.5s | Cost = 483.37\nEpoch 23 | Time = 0.5s | Cost = 474.37\nEpoch 24 | Time = 0.5s | Cost = 465.37\nEpoch 25 | Time = 0.6s | Cost = 456.37\nEpoch 26 | Time = 0.6s | Cost = 447.37\nEpoch 27 | Time = 0.6s | Cost = 438.37\nEpoch 28 | Time = 0.7s | Cost = 429.37\nEpoch 29 | Time = 0.7s | Cost = 420.37\nEpoch 30 | Time = 0.7s | Cost = 411.37\nEpoch 31 | Time = 0.7s | Cost = 402.37\nEpoch 32 | Time = 0.7s | Cost = 393.37\nEpoch 33 | Time = 0.8s | Cost = 384.37\nEpoch 34 | Time = 0.8s | Cost = 375.37\nEpoch 35 | Time = 0.8s | Cost = 366.37\nEpoch 36 | Time = 0.8s | Cost = 357.37\nEpoch 37 | Time = 0.9s | Cost = 348.37\nEpoch 38 | Time = 0.9s | Cost = 339.37\nEpoch 39 | Time = 0.9s | Cost = 330.37\nEpoch 40 | Time = 0.9s | Cost = 321.37\nEpoch 41 | Time = 1.0s | Cost = 312.38\nEpoch 42 | Time = 1.0s | Cost = 303.38\nEpoch 43 | Time = 1.0s | Cost = 305.62\nEpoch 44 | Time = 1.0s | Cost = 303.38\nEpoch 45 | Time = 1.1s | Cost = 305.62\nEpoch 46 | Time = 1.1s | Cost = 303.38\nEpoch 47 | Time = 1.1s | Cost = 305.62\nEpoch 48 | Time = 1.1s | Cost = 303.38\nEpoch 49 | Time = 1.2s | Cost = 305.62\nEpoch 50 | Time = 1.2s | Cost = 303.38\nEpoch 51 | Time = 1.2s | Cost = 305.62\nEpoch 52 | Time = 1.2s | Cost = 303.38\nEpoch 53 | Time = 1.3s | Cost = 305.62\nEpoch 54 | Time = 1.3s | Cost = 303.38\nEpoch 55 | Time = 1.3s | Cost = 305.62\nEpoch 56 | Time = 1.3s | Cost = 303.38\nEpoch 57 | Time = 1.4s | Cost = 305.62\nEpoch 58 | Time = 1.4s | Cost = 303.38\nEpoch 59 | Time = 1.4s | Cost = 305.62\nEpoch 60 | Time = 1.4s | Cost = 303.38\nEpoch 61 | Time = 1.5s | Cost = 305.62\nEpoch 62 | Time = 1.5s | Cost = 303.38\nEpoch 63 | Time = 1.5s | Cost = 305.62\nEpoch 64 | Time = 1.5s | Cost = 303.38\nEpoch 65 | Time = 1.6s | Cost = 305.62\nEpoch 66 | Time = 1.6s | Cost = 303.38\nEpoch 67 | Time = 1.6s | Cost = 305.62\nEpoch 68 | Time = 1.6s | Cost = 303.38\nEpoch 69 | Time = 1.7s | Cost = 305.62\nEpoch 70 | Time = 1.7s | Cost = 303.38\nEpoch 71 | Time = 1.7s | Cost = 305.62\nEpoch 72 | Time = 1.7s | Cost = 303.38\nEpoch 73 | Time = 1.8s | Cost = 305.62\nEpoch 74 | Time = 1.8s | Cost = 303.38\nEpoch 75 | Time = 1.8s | Cost = 305.62\nEpoch 76 | Time = 1.8s | Cost = 303.38\nEpoch 77 | Time = 1.9s | Cost = 305.62\nEpoch 78 | Time = 1.9s | Cost = 303.38\nEpoch 79 | Time = 1.9s | Cost = 305.62\nEpoch 80 | Time = 1.9s | Cost = 303.38\nEpoch 81 | Time = 2.0s | Cost = 305.62\nEpoch 82 | Time = 2.0s | Cost = 303.38\nEpoch 83 | Time = 2.0s | Cost = 305.62\nEpoch 84 | Time = 2.0s | Cost = 303.38\nEpoch 85 | Time = 2.1s | Cost = 305.62\nEpoch 86 | Time = 2.1s | Cost = 303.38\nEpoch 87 | Time = 2.1s | Cost = 305.62\nEpoch 88 | Time = 2.1s | Cost = 303.38\nEpoch 89 | Time = 2.2s | Cost = 305.62\nEpoch 90 | Time = 2.2s | Cost = 303.38\nEpoch 91 | Time = 2.2s | Cost = 305.62\nEpoch 92 | Time = 2.2s | Cost = 303.38\nEpoch 93 | Time = 2.3s | Cost = 305.62\nEpoch 94 | Time = 2.3s | Cost = 303.38\nEpoch 95 | Time = 2.3s | Cost = 305.62\nEpoch 96 | Time = 2.3s | Cost = 303.38\nEpoch 97 | Time = 2.4s | Cost = 305.62\nEpoch 98 | Time = 2.4s | Cost = 303.38\nEpoch 99 | Time = 2.4s | Cost = 305.62\nEpoch 100 | Time = 2.4s | Cost = 303.38\nApplicationDrivenLearning.Solution(303.3750343322754, Real[19.887499f0])","title":"Example"},{"category":"section","location":"tutorials/modes/#Pros-3","page":"Training modes","text":"Tends to require fewer iterations to achieve good results.\nCan use different optimizers from Flux.jl and strategies such as stochastic gradient descent with small batches.","title":"Pros"},{"category":"section","location":"tutorials/modes/#Cons-3","page":"Training modes","text":"Relies on the gradient of the objective function, which can be costly to compute, sensitive to the learning rate and on the initial guess.\nDoes not support integer variables in the plan and assess models.","title":"Cons"},{"category":"section","location":"examples/scheduling/#Minimal-Scheduling-Problem","page":"Minimal Scheduling Problem","text":"This example shows how to use the ApplicationDrivenLearning.jl package to solve a minimal scheduling problem.\n\nIn this problem, we have to define the dispatch of a single unit, considering its operational constraints, costs and the demand. The cost for under-dispatching is 100 and the cost for over-dispatching is 20. The forecast model will be a simple linear model.\n\nThe plan model will only assign the dispatch of the unit equal to the forecast. The assess model will apply a correction to the dispatch, considering the under-dispatching and over-dispatching costs.\n\nFirst, let's load the necessary packages.\n\nusing Flux\nusing JuMP\nusing Gurobi\nusing ApplicationDrivenLearning\nusing Optim\n\nADL = ApplicationDrivenLearning\n\nNow, we can initialize the application driven learning model, build the plan and assess models and set the forecast model.\n\n# init application driven learning model\nmodel = ADL.Model()\n@variable(model, z >= 0, ADL.Policy)\n@variable(model, y, ADL.Forecast)\n\n# plan model\n@constraints(ADL.Plan(model), begin\n    z.plan == y.plan\nend)\n@objective(ADL.Plan(model), Min, 10*z.plan)\n\n# assess model\n@variables(ADL.Assess(model), begin\n    correction\n    under_dispatch >= 0\n    over_dispatch >= 0\nend)\n@constraints(ADL.Assess(model), begin\n    correction == y.assess - z.assess\n    under_dispatch >= correction\n    over_dispatch >= -correction\nend)\n@objective(ADL.Assess(model), Min, 10*y.assess + 100*under_dispatch + 20*over_dispatch)\n\n# forecast model\nADL.set_forecast_model(\n    model,\n    ADL.ForecastModel(\n        architecture = Dense(1 => 1, exp; bias=false),\n        outputs = [y],\n    ),\n)\n\nWe can check how the model performs by computing the assess cost with the initial (random) forecast model.\n\nX = ones(2, 1) .|> Float32\nY = Dict(y => [0.0, 2.0] .|> Float32)\nset_optimizer(model, Gurobi.Optimizer)\nset_silent(model)\n\njulia> ADL.compute_cost(model, X, Y)\n99.7323203086853\n\nAnd finally, we can train the model with OptimMode over Nelder-Mead.\n\njulia> solution = ADL.train!(\n    model, X, Y,\n    ADL.Options(\n        ADL.OptimMode,\n        algorithm=Optim.NelderMead(),\n        iterations=100,\n        show_trace=true\n    )\n)\nIter     Function value    √(Σ(yᵢ-ȳ)²)/n \n------   --------------    --------------\n     0     9.973232e+01     2.466946e+00\n * time: 0.0\n     1     9.973232e+01     3.148899e+01\n * time: 0.006000041961669922\n     2     3.675434e+01     1.421373e+01\n * time: 0.014999866485595703\n     3     3.675434e+01     2.673199e+00\n * time: 0.0279998779296875\n     4     3.140795e+01     6.259584e-01\n * time: 0.039999961853027344\n     5     3.015603e+01     1.546907e-01\n * time: 0.04699993133544922\n     6     3.015603e+01     3.849030e-02\n * time: 0.05299997329711914\n     7     3.007905e+01     3.841400e-02\n * time: 0.05799984931945801\n     8     3.000222e+01     9.596825e-03\n * time: 0.06699991226196289\n     9     3.000222e+01     2.398491e-03\n * time: 0.07599997520446777\n    10     3.000222e+01     5.979546e-04\n * time: 0.0839998722076416\n    11     3.000103e+01     3.366484e-04\n * time: 0.08899998664855957\n    12     3.000035e+01     1.144409e-04\n * time: 0.09500002861022949\n    13     3.000012e+01     3.814697e-05\n * time: 0.10699987411499023\n    14     3.000005e+01     9.536743e-06\n * time: 0.11500000953674316\n    15     3.000003e+01     9.536743e-06\n * time: 0.12199997901916504\n    16     3.000001e+01     9.536743e-06\n * time: 0.1269998550415039\n    17     2.999999e+01     3.015783e-06\n * time: 0.13899993896484375\n    18     2.999999e+01     3.015783e-06\n * time: 0.14399981498718262\n    19     2.999998e+01     1.907349e-06\n * time: 0.15400004386901855\n    20     2.999998e+01     0.000000e+00\n * time: 0.1640000343322754\nApplicationDrivenLearning.Solution(29.99998f0, Real[0.6931467f0])\n\njulia> model.forecast(X[1,:])  # final forecast\n1-element Vector{Float32}:\n 1.999999\n\njulia> ADL.compute_cost(model, X, Y)  # final cost\n29.99998","title":"Minimal Scheduling Problem"},{"category":"section","location":"tutorials/custom_forecast/#Custom-forecast-models","page":"Custom forecast models","text":"The basic approach to define a forecast model is to use a Chain from the Flux.jl package, that directly maps the input to the output. But there are cases where this approach is not enough.\n\nA forecast is described by ForecastModel units. Each unit is one architecture together with the input columns it reads and the forecast variables it predicts:\n\nForecastModel(\n    inputs = [:temp, :hour],     # column positions or column names\n    architecture = Dense(2 => 1),\n    outputs = [demand],          # the Forecast variables this architecture predicts\n)\n\nA vector of units goes to set_forecast_model, which checks that between them they predict every forecast variable declared on the model, exactly once.","title":"Custom forecast models"},{"category":"section","location":"tutorials/custom_forecast/#Wiring-inputs-to-forecast-variables","page":"Custom forecast models","text":"The connection between predictive model outputs and plan model inputs is not always a straightforward one. Writing one unit per architecture is what makes it explicit: a unit says which columns it reads, so the same prediction logic can be applied across several entities (such as production units or geographical locations) by declaring it once per entity.\n\nConsider a scenario where the input dataset contains 3 variables (for example expected temperature on location 1, expected temperature on location 2 and weekday), there are 2 forecast variables (energy demand on the two locations of interest) and the forecast model should use only the expected temperature of a location to predict its demand. That is two units of the same shape, one per location.\n\nmodel = ApplicationDrivenLearning.Model()\n@variable(model, demand[1:2], ApplicationDrivenLearning.Forecast)\n\nX = [\n    76 89 2;\n    72 85 3\n] .|> Float32 # input dataset of size 2 by 3\nY = Dict(\n    demand[1] => [101, 89] .|> Float32,\n    demand[2] => [68, 49] .|> Float32\n)\n\n# the architecture each location uses: 2 inputs, one output value\ndem_forecast = Dense(2 => 1)\n\nApplicationDrivenLearning.set_forecast_model(\n    model,\n    [\n        ForecastModel(\n            inputs = [1, 3],              # temperature at location 1, weekday\n            architecture = dem_forecast,\n            outputs = [demand[1]],\n        ),\n        ForecastModel(\n            inputs = [2, 3],              # temperature at location 2, weekday\n            architecture = deepcopy(dem_forecast),\n            outputs = [demand[2]],\n        ),\n    ],\n)\n\nNote the deepcopy. Each unit trains its own parameters, so reusing an architecture across units means reusing its shape, not its weights — which is what \"the same model for each location\" almost always means, and what lets the two locations learn different sensitivities to temperature. Handing the very same object to two units is rejected rather than quietly tying their weights together:\n\n# ERROR: Units 1 and 2 hold the same `architecture` object ...\nForecastModel(inputs = [1, 3], architecture = dem_forecast, outputs = [demand[1]]),\nForecastModel(inputs = [2, 3], architecture = dem_forecast, outputs = [demand[2]]),\n\nA function returning a fresh network reads better when there are more than two:\n\ndem_forecast() = Dense(2 => 1)\nunits = [\n    ForecastModel(\n        inputs = [i, 3],\n        architecture = dem_forecast(),\n        outputs = [demand[i]],\n    ) for i in 1:2\n]","title":"Wiring inputs to forecast variables"},{"category":"section","location":"tutorials/custom_forecast/#Naming-the-inputs","page":"Custom forecast models","text":"Integer indexes are positions in X, so [1, 3] only means \"temperature at location 1 and the weekday\" as long as the columns arrive in that order. A unit's inputs can be written as column names instead, which says what it means and lets X be given as a table:\n\nApplicationDrivenLearning.set_forecast_model(\n    model,\n    [\n        ForecastModel(\n            inputs = [:temp_1, :weekday],\n            architecture = dem_forecast(),\n            outputs = [demand[1]],\n        ),\n        ForecastModel(\n            inputs = [:temp_2, :weekday],\n            architecture = dem_forecast(),\n            outputs = [demand[2]],\n        ),\n    ],\n)\n\nusing DataFrames\nX = DataFrame(temp_1 = [76, 89], temp_2 = [72, 85], weekday = [2, 3])\n\nThe names become the forecast's input schema and are resolved to positions once, so nothing else changes: a DataFrame may now hold its columns in any order, and any column it is missing is reported by name. A matrix X is still read positionally, in the order the units first mention the names — so writing the units in a different order is how a different column order is asked for. There is no separate place to declare the schema: naming a unit's inputs is the declaration.\n\nEvery unit has to select the same way, all by position or all by name, since there is one input schema for the whole forecast. And no unit may read the same column twice, which would feed two of its architecture's inputs from one column.","title":"Naming the inputs"},{"category":"section","location":"tutorials/custom_forecast/#Several-architectures-in-one-forecast","page":"Custom forecast models","text":"Units may hold different architectures, which supports the modular construction of predictive models where specialized components forecast different aspects of the problem, without the difficulty of defining custom architectures.\n\nUsing the context from the previous example, let's assume we also want to predict price for each location using a single model that receives average lagged price. That is one more unit, with its own architecture and its own outputs.\n\nmodel = ApplicationDrivenLearning.Model()\n@variables(model, begin\n    demand[1:2], ApplicationDrivenLearning.Forecast\n    price[1:2], ApplicationDrivenLearning.Forecast\nend)\n\nX = [\n    76 89 2 103;\n    72 85 3 89\n] .|> Float32  # input dataset of size 2 by 4\nY = Dict(\n    demand[1] => [101, 89] .|> Float32,\n    demand[2] => [68, 49] .|> Float32,\n    price[1] => [101, 89] .|> Float32,\n    price[2] => [68, 49] .|> Float32,\n)\n\nApplicationDrivenLearning.set_forecast_model(\n    model,\n    [\n        # each demand variable from its own location's temperature and the weekday\n        ForecastModel(\n            inputs = [1, 3],\n            architecture = Dense(2 => 1),\n            outputs = [demand[1]],\n        ),\n        ForecastModel(\n            inputs = [2, 3],\n            architecture = Dense(2 => 1),\n            outputs = [demand[2]],\n        ),\n        # and both prices from the lagged price, by one architecture with two\n        # outputs - listed in the order it produces them\n        ForecastModel(\n            inputs = [4],\n            architecture = Dense(1 => 2),\n            outputs = [price[1], price[2]],\n        ),\n    ],\n)\n\nA unit with several outputs is the case where one architecture predicts several variables at once: Dense(1 => 2) produces two values from the lagged price, and outputs says which variable each row is. The number of outputs is checked against the architecture when the unit is built, so a Dense(1 => 2) listing three variables is an error there rather than a shape mismatch during training.","title":"Several architectures in one forecast"},{"category":"section","location":"tutorials/custom_forecast/#Naming-the-columns-of-Y","page":"Custom forecast models","text":"Container declarations like @variable(model, demand[1:2], Forecast) name their variables demand[1] and demand[2], which no data file is likely to carry. Write the Y column next to the variable it holds:\n\nForecastModel(\n    inputs = [1, 3],\n    architecture = Dense(2 => 1),\n    outputs = [demand[1] => :demand_loc1],\n)\n\nThe column name then travels with its variable, so there is no separate list to keep in the right order. A bare variable means \"the column is named after the variable\", which is what @variable(model, demand, Forecast) already gives you.","title":"Naming the columns of Y"},{"category":"section","location":"tutorials/custom_forecast/#Pinning-the-columns-of-a-matrix-Y","page":"Custom forecast models","text":"A name addresses a column of a named container — a table, a DataFrame, a Dict of series. A matrix has no names, so its columns are read in the order the Forecast variables were declared on the model. That works, but nothing states it and nothing checks it: insert a @variable above the others and every caller passing a matrix silently means something else.\n\nWrite the position instead, and the layout is declared:\n\nForecastModel(\n    architecture = Dense(2 => 2),\n    outputs = [demand[1] => 2, demand[2] => 1],   # Y column 2 holds demand[1]\n)\n\nPositions behave like the ones in inputs. They start at 1, no two variables may share one, and gaps are allowed — outputs = [a => 1, b => 3] requires a three-column Y and never looks at column 2. A Y too narrow to reach the widest declared column is an error rather than a guess.\n\nThe two spellings say the same thing, so all units of a forecast use one or the other, and a positional forecast refuses a named container: a table's columns have names, and matching those by order is exactly what this design refuses to do. Pass a matrix, or address the columns by name.","title":"Pinning the columns of a matrix Y"},{"category":"section","location":"tutorials/getting_started/#Getting-started-with-ApplicationDrivenLearning","page":"Getting started with ApplicationDrivenLearning","text":"This is a quick introduction to modeling and training end-to-end forecast models with ApplicationDrivenLearning.","title":"Getting started with ApplicationDrivenLearning"},{"category":"section","location":"tutorials/getting_started/#A-first-example","page":"Getting started with ApplicationDrivenLearning","text":"We will train an energy load predictive model that is applied to a one-hour-ahead generation planning problem in a power system consisting of a single plant with the following characteristics: a production capacity of 4 MW and a generation cost of R$10/MWh.\n\nThe available data is very limited: we don't have any auxiliary variable and just two samples of past demand.\n\nHere is the complete code to model, train and extract the parameters of the predictive model:\n\nusing JuMP\nusing Flux\nimport HiGHS\nusing ApplicationDrivenLearning\nusing Optim\n\n# main model and policy / forecast variables\nmodel = ApplicationDrivenLearning.Model()\n@variables(model, begin\n    z, ApplicationDrivenLearning.Policy\n    θ, ApplicationDrivenLearning.Forecast\nend)\n\n# plan model\n@variables(ApplicationDrivenLearning.Plan(model), begin\n    c1 ≥ 0\n    c2 ≥ 0\nend)\n@constraints(ApplicationDrivenLearning.Plan(model), begin\n    c1 ≥ 100 * (θ.plan-z.plan)\n    c2 ≥ 20 * (z.plan-θ.plan)\nend)\n@objective(ApplicationDrivenLearning.Plan(model), Min, 10*z.plan + c1 + c2)\n\n# assess model\n@variables(ApplicationDrivenLearning.Assess(model), begin\n    c3 ≥ 0\n    c4 ≥ 0\nend)\n@constraints(ApplicationDrivenLearning.Assess(model), begin\n    c3 ≥ 100 * (θ.assess-z.assess)\n    c4 ≥ 20 * (z.assess-θ.assess)\nend)\n@objective(ApplicationDrivenLearning.Assess(model), Min, 10*z.assess + c3 + c4)\n\n# basic setting\nset_optimizer(model, HiGHS.Optimizer)\nset_silent(model)\n\n# data\nX = reshape([1 1], (2, 1)) .|> Float32\nY = Dict(θ => [10, 20] .|> Float32)\n\n# forecast model: one architecture, and the forecast variable it predicts\nApplicationDrivenLearning.set_forecast_model(\n    model,\n    ApplicationDrivenLearning.ForecastModel(\n        architecture = Chain(Dense(1 => 1; bias=false)),\n        outputs = [θ],\n    ),\n)\n\n# training the full model\nsolution = ApplicationDrivenLearning.train!(\n    model,\n    X,\n    Y,\n    ApplicationDrivenLearning.Options(\n        ApplicationDrivenLearning.OptimMode;\n        algorithm = Optim.NelderMead(),\n    )\n)\n\n# getting predictions\npred = model.forecast(X')\n\n# extracting solution\nprintln(solution.params)","title":"A first example"},{"category":"section","location":"tutorials/getting_started/#Step-by-step","page":"Getting started with ApplicationDrivenLearning","text":"Once installed, the necessary packages can be loaded into julia:\n\nusing JuMP\nusing Flux\nusing ApplicationDrivenLearning\nusing Optim\n\nWe have to include a solver for solving the optimization models. In this case, we load HiGHS:\n\nusing HiGHS\n\nJust like regular JuMP, ApplicationDrivenLearning has a Model function to initialize an empty model. After initializing, we can declare the policy and forecast variables.\n\nPolicy variables represent decision variables that should be maintained from the Plan to the Assess model.\nForecast variables represent future values relevant to the problem. They are replaced by forecast output values in the planning step and fixed as realized values (from the Y matrix) in the assessment step.\n\nFor our problem, the policy variable z represents the generation and forecast variable θ represents the demand.\n\nmodel = ApplicationDrivenLearning.Model()\n@variables(model, begin\n    z, ApplicationDrivenLearning.Policy\n    θ, ApplicationDrivenLearning.Forecast\nend)\n\nTo populate the plan model, we follow a syntax very similar to the JuMP package, with the addition of a suffix on policy and forecast variables.\n\nWe declare additional variables c1 and c2 to model the overestimation and underestimation costs, that are added to the generation cost in the objective function. \n\n@variables(ApplicationDrivenLearning.Plan(model), begin\n    c1 ≥ 0\n    c2 ≥ 0\nend)\n@constraints(ApplicationDrivenLearning.Plan(model), begin\n    c1 ≥ 100 * (θ.plan-z.plan)\n    c2 ≥ 20 * (z.plan-θ.plan)\nend)\n@objective(ApplicationDrivenLearning.Plan(model), Min, 10*z.plan + c1 + c2)\n\nThe assess model can be declared in a similar way.\n\n@variables(ApplicationDrivenLearning.Assess(model), begin\n    c3 ≥ 0\n    c4 ≥ 0\nend)\n@constraints(ApplicationDrivenLearning.Assess(model), begin\n    c3 ≥ 100 * (θ.assess-z.assess)\n    c4 ≥ 20 * (z.assess-θ.assess)\nend)\n@objective(ApplicationDrivenLearning.Assess(model), Min, 10*z.assess + c3 + c4)\n\nWe need to associate the model with an optimizer that can solve the plan and assess models. For this case, we use the HiGHS optimizer. We also set the model to silent mode to avoid excessive outputs from the solve iterations.\n\nset_optimizer(model, HiGHS.Optimizer)\nset_silent(model)\n\nAs explained, the data used to train the model is very limited, composed of only two samples of energy demand. Values of one are used as input data, without adding any real additional information to the model. X is a matrix representing input values and the dictionary Y maps the forecast variable θ to numerical values to be used. Both X and Y values are transformed to Float32 type to match Flux parameters.\n\nX = reshape([1 1], (2, 1)) .|> Float32\nY = Dict(θ => [10, 20] .|> Float32)","title":"Step-by-step"},{"category":"section","location":"tutorials/getting_started/#Accepted-input-containers","page":"Getting started with ApplicationDrivenLearning","text":"X and Y are not restricted to matrices and dictionaries. Both compute_cost and train! accept:\n\na matrix of size (samples, features);\na vector, read as a single column;\nany Tables.jl-compatible table — a DataFrame, a NamedTuple of vectors, a CSV.File, and so on;\nfor Y only, a Dict mapping each forecast variable to its series, as above.\n\nThe two arguments are independent, so a DataFrame for X and a Dict for Y is fine. So the example above could equally be written:\n\nusing DataFrames\nX = DataFrame(ones = Float32[1, 1])\nY = DataFrame(θ = Float32[10, 20])\n\n# a table is read by its column names, so name the column the unit reads\nApplicationDrivenLearning.set_forecast_model(\n    model,\n    ApplicationDrivenLearning.ForecastModel(\n        inputs = [:ones],\n        architecture = Chain(Dense(1 => 1; bias=false)),\n        outputs = [θ],\n    ),\n)","title":"Accepted input containers"},{"category":"section","location":"tutorials/getting_started/#How-columns-are-matched","page":"Getting started with ApplicationDrivenLearning","text":"One rule covers both arguments:\n\nA named container is matched by name; an unnamed container is matched by position. Neither is ever guessed at.\n\nAn array carries no column names, so position is its only possible reading. A table's columns are named, and those names are what gets used — so reordering the columns of a DataFrame cannot change your results, and extra columns (an id or a date, say) are ignored. If the names cannot be matched, you get an error rather than a silent fallback to column order, which is how you would otherwise end up training against the wrong series without any warning.\n\nMatrix(df) is how you ask for positional matching, and reads as exactly that request: it drops the names.\n\nThe names a table is matched against are the ones the model declares:\n\nY uses the names of the forecast variables. @variable(model, θ, Forecast) reads a θ column with nothing to configure. Container declarations like @variable(model, θ[1:2], Forecast) name their variables θ[1] and θ[2], which no table is likely to carry, so name the column next to the variable — outputs = [θ[1] => :demand, θ[2] => :price] — or use the Dict form, which is keyed by the variables themselves.\nX uses the column names in each unit's inputs. Writing them as names rather than as positions is what declares the input schema, and there is no other place to declare it:\n\nApplicationDrivenLearning.set_forecast_model(\n    model,\n    [\n        ForecastModel(\n            inputs = [:temp, :hour],\n            architecture = Dense(2 => 1),\n            outputs = [demand],\n        ),\n        ForecastModel(\n            inputs = [:price],\n            architecture = Dense(1 => 1),\n            outputs = [spill],\n        ),\n    ],\n)\n\nA unit whose inputs are positions — or nothing, meaning the whole row — declares no names, so a table handed to that model is refused rather than read by column order. Matrix(X) is how you ask for that reading deliberately.\n\nThere is no exception for a table with a single column, which is why the DataFrame version of this tutorial's data names the column its unit reads. Its order cannot be wrong, but its name still can: a one-input model handed a humidity column when it wanted temp is a mistake worth catching, and only a declared schema catches it. Y needs nothing extra there, since @variable(model, θ, Forecast) already names its own column.","title":"How columns are matched"},{"category":"section","location":"tutorials/getting_started/#How-rows-are-matched","page":"Getting started with ApplicationDrivenLearning","text":"The same rule extends one dimension further. By default X and Y are lined up by row order, and nothing checks that row t of one is the same observation as row t of the other — only that they have the same number of rows. Naming the column that identifies an observation fixes that:\n\nApplicationDrivenLearning.set_forecast_model(\n    model,\n    ApplicationDrivenLearning.ForecastModel(\n        inputs = [:ones],\n        architecture = Chain(Dense(1 => 1; bias=false)),\n        outputs = [θ],\n    );\n    sample_key = :timestamp,\n)\n\nX = DataFrame(timestamp = [10, 20, 30], ones = Float32[1, 1, 1])\nY = DataFrame(timestamp = [30, 10, 20], θ = Float32[30, 10, 20])  # any order\n\nThe realized values are then looked up by key for each row of X, so Y may arrive in any order, and Y may hold samples that X does not ask for — the row-wise counterpart of ignoring an unwanted column. A sample that X asks for and Y lacks is an error rather than a silent off-by-one, and a key that repeats is rejected because it cannot identify a sample.\n\nX fixes the order, since that is the order the returned per-sample costs and gradients are in. The key column is not a feature: it identifies a row, so it is never fed to the network. And as on the column side, a container that carries no row labels — a matrix, or the Dict form of Y — falls back to order.\n\nColumns must be numeric. A column of strings, or one containing missing, is rejected with an error naming the offending argument rather than failing later inside the solver.\n\nA simple forecast model with only one parameter can be defined as a Flux.Dense layer with just 1 weight and no bias. A ForecastModel pairs that architecture with the forecast variables it predicts, and set_forecast_model checks that between them the units predict every declared forecast variable exactly once.\n\nApplicationDrivenLearning.set_forecast_model(\n    model,\n    ApplicationDrivenLearning.ForecastModel(\n        architecture = Chain(Dense(1 => 1; bias=false)),\n        outputs = [θ],\n    ),\n)\n\nFinally, the full model is trained with OptimMode, which reaches any algorithm from the Optim package — here Nelder-Mead. Optim is already a dependency of ApplicationDrivenLearning, so it does not need to be installed, but it does need using Optim to name the algorithm.\n\nsolution = ApplicationDrivenLearning.train!(\n    model,\n    X,\n    Y,\n    ApplicationDrivenLearning.Options(\n        ApplicationDrivenLearning.OptimMode;\n        algorithm = Optim.NelderMead(),\n    )\n)\n\nNow we can easily make new predictions with the trained model:\n\njulia> pred = model.forecast(X')\n1×2 Matrix{Float32}:\n 20.0  20.0\n\nCompute the assess cost for each data sample:\n\njulia> ApplicationDrivenLearning.compute_cost(model, X, Y, false, false)\n2-element Vector{Float64}:\n 400.0\n 200.0\n\nAnd also extract the parameters from the trained model:\n\njulia> solution.params\n1-element Vector{Real}:\n 20.0f0","title":"How rows are matched"},{"category":"section","location":"reference/#API","page":"API Reference","text":"This section documents the public ApplicationDrivenLearning API.\n\nFunctions and types whose name starts with an underscore are internal implementation details: they are not part of the public API, are not listed here, and may change without a breaking release.","title":"API"},{"category":"section","location":"reference/#Constructors","page":"API Reference","text":"","title":"Constructors"},{"category":"section","location":"reference/#JuMP-variable-types","page":"API Reference","text":"","title":"JuMP variable types"},{"category":"section","location":"reference/#Structs","page":"API Reference","text":"","title":"Structs"},{"category":"section","location":"reference/#Modes","page":"API Reference","text":"","title":"Modes"},{"category":"section","location":"reference/#Deprecated-Modes","page":"API Reference","text":"","title":"Deprecated Modes"},{"category":"section","location":"reference/#Parallel-backends","page":"API Reference","text":"","title":"Parallel backends"},{"category":"section","location":"reference/#Attributes-getters-and-setters","page":"API Reference","text":"","title":"Attributes getters and setters"},{"category":"section","location":"reference/#Other-functions","page":"API Reference","text":"","title":"Other functions"},{"category":"type","location":"reference/#ApplicationDrivenLearning.Model","page":"API Reference","text":"Model <: JuMP.AbstractModel\n\nCreate an empty ApplicationDrivenLearning.Model with empty plan and assess models, missing forecast model and default settings.\n\nBesides the Policy and Forecast variable pairs, the model keeps the plan-side and assess-side variables split into their own concretely typed vectors. Those are what the per-sample cost evaluation actually iterates over, so they are maintained as variables are declared rather than rebuilt on every access.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.Model"},{"category":"type","location":"reference/#ApplicationDrivenLearning.ForecastModel","page":"API Reference","text":"ForecastModel(; inputs = nothing, architecture, outputs)\n\nOne forecast model: an architecture, the input columns it reads and the Forecast variables it predicts.\n\nA forecast is described by a vector of these, handed to set_forecast_model, which assembles them into the FullForecastModel stored on the Model. Everything in a unit is about the same one architecture, so there are no parallel vectors to keep aligned and no map iteration order to know.\n\nArguments\n\ninputs: the input columns this architecture reads, as column positions ([1, 3]) or column names ([:temp, :hour]). A scalar is accepted for a one-input architecture. nothing means \"the whole input row\", which is only resolvable when the row width is known — either because this is the only unit, or because another unit selects by name and so fixes the schema. Writing the names here is what declares the input schema; there is no separate place to declare it.\narchitecture: anything callable on a (features x samples) matrix — a Flux.Dense, a Flux.Chain, or your own layer. It is not copied here, so that reusing one object across units can be reported as an error; it is deep-copied when the units are assembled, so training never touches the caller's object.\noutputs: the Forecast variables this architecture predicts, in the order it produces them. Say where a variable's realized values live in Y by writing variable => :column for a named container, or variable => position for a matrix; either way the answer sits next to its variable, so there is no second ordering to line up. Left unsaid, a variable reads the Y column carrying its own declared name.\nA name and a position are two spellings of one thing, so all units of a forecast use one or the other. Positions are the only way to pin the layout of a matrix Y: without them its columns are read in the order the Forecast variables were declared on the Model, which nothing states and nothing checks.\n\nAll units of one forecast must select their inputs the same way — all by position or all by name — since a mix leaves the input schema undefined.\n\nExamples\n\nOne network reading two named columns:\n\nmodel = ApplicationDrivenLearning.Model()\n@variable(model, demand, ApplicationDrivenLearning.Forecast)\n\nset_forecast_model(\n    model,\n    ForecastModel(\n        inputs = [:temp, :hour],\n        architecture = Flux.Dense(2 => 1),\n        outputs = [demand],\n    ),\n)\n\nOne network per forecast variable, each with its own parameters, naming the Y columns because the variables are called load[1] and load[2]:\n\n@variable(model, load[1:2], ApplicationDrivenLearning.Forecast)\n\nset_forecast_model(\n    model,\n    [\n        ForecastModel(\n            inputs = :temp,\n            architecture = Flux.Dense(1 => 1),\n            outputs = [load[1] => :load_a],\n        ),\n        ForecastModel(\n            inputs = :wind,\n            architecture = Flux.Dense(1 => 1),\n            outputs = [load[2] => :load_b],\n        ),\n    ],\n)\n\nTo reuse one architecture for several forecasts, give each unit its own copy — deepcopy(nn), or a function returning a fresh network. Passing the same object to two units is an error rather than a silent choice between \"same shape\" and \"same weights\".\n\n\n\n\n\n","title":"ApplicationDrivenLearning.ForecastModel"},{"category":"type","location":"reference/#ApplicationDrivenLearning.FullForecastModel","page":"API Reference","text":"FullForecastModel(units, output_variables, input_size, output_size; input_names, output_columns, sample_key)\n\nThe assembled forecast of a Model: every architecture, the input columns each reads, the Forecast variables each predicts, and the schema of the data containers.\n\nBuilt by set_forecast_model from a vector of ForecastModel units, which is how it should be constructed — this explicit form exists because the fields are what the prediction loop, the optimizers and the parallel backends work on, and because Functors rebuilds the struct positionally.\n\nArguments\n\nunits: the ResolvedUnits, one per ForecastModel the forecast was assembled from. Each carries its own architecture, the input columns it reads and the prediction rows it writes, so there are no parallel vectors to keep in step. The architectures are deep-copied when the units are assembled, so the caller's objects are left untouched.\noutput_variables: the forecast variables in the order the model produces them, i.e. the row order of a prediction. Every row is written by exactly one unit, which is checked here.\ninput_size::Int: number of columns the input has.\noutput_size::Int: number of forecast variables.\ninput_names::Union{Vector{Symbol},Nothing}: name of each input column, in the order the model expects them. This is the model's declared input schema: it is what lets a table input be matched by name instead of by column order. nothing means the model declares no schema, and a table input is then rejected rather than guessed at.\noutput_columns::Union{Vector{Symbol},Vector{Int},Nothing}: where in Y each variable of output_variables is, in that same order — as table column names, or as column positions in a matrix. nothing means the declared names of the Forecast variables themselves are used. Assembled from the variable => :column and variable => position pairs of the units, which is also why only one of the two spellings can be present.\nsample_key::Union{Symbol,Nothing}: name of the column that identifies a row, the way input_names identifies a column — a timestamp or an id. When both X and Y are tables carrying it, the realized values are looked up by key for each row of X instead of being trusted to arrive in the same order. nothing means rows are matched by position.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.FullForecastModel"},{"category":"function","location":"reference/#ApplicationDrivenLearning.Plan","page":"API Reference","text":"Plan(model::ApplicationDrivenLearning.Model)\n\nReturn the inner JuMP.Model used for planning, i.e. the problem solved with the predictive model output in place of the Forecast variables. Use it as the target of the @variable, @constraint and @objective macros.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.Plan"},{"category":"function","location":"reference/#ApplicationDrivenLearning.Assess","page":"API Reference","text":"Assess(model::ApplicationDrivenLearning.Model)\n\nReturn the inner JuMP.Model used for assessment, i.e. the problem solved with the realized values in place of the Forecast variables and the Policy variables fixed to the plan decision. Use it as the target of the @variable, @constraint and @objective macros.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.Assess"},{"category":"type","location":"reference/#ApplicationDrivenLearning.Policy","page":"API Reference","text":"Policy{T}\n\nPolicy variable type that holds plan and assess variables.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.Policy"},{"category":"type","location":"reference/#ApplicationDrivenLearning.Forecast","page":"API Reference","text":"Forecast{T}\n\nForecast variable type that holds plan and assess variables.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.Forecast"},{"category":"type","location":"reference/#ApplicationDrivenLearning.Options","page":"API Reference","text":"Options(mode; params...)\n\nOptions struct to hold optimization mode and mode parameters.\n\nmode must be a subtype of AbstractOptimizationMode (the type itself, not an instance); the accepted keyword arguments are documented on each mode.\n\n...\n\nExample\n\noptions = Options(\n    GradientMode;\n    rule = Flux.RMSProp(0.01),\n    epochs = 100,\n    batch_size = 10,\n)\n\n...\n\n\n\n\n\n","title":"ApplicationDrivenLearning.Options"},{"category":"type","location":"reference/#ApplicationDrivenLearning.Solution","page":"API Reference","text":"Solution{C<:Real,P<:Real}\n\nResult of a train! call.\n\nThe cost and the parameters are stored with their own concrete element types, which usually differ: costs come from the solver and are Float64, while parameters follow the precision of the Flux model (often Float32).\n\n...\n\nFields\n\ncost::C: best assessed cost found during training, averaged over the training samples.\nparams::Vector{P}: predictive model parameters that achieve cost, flattened in the order used by extract_params. The model itself is also updated in place with these values. ...\n\n\n\n\n\n","title":"ApplicationDrivenLearning.Solution"},{"category":"type","location":"reference/#ApplicationDrivenLearning.OptimMode","page":"API Reference","text":"OptimMode <: AbstractOptimizationMode\n\nUsed to solve the application driven learning training problem with any algorithm from the Optim.jl package.\n\nThe objective handed to Optim is the assessed cost as a function of the flat parameter vector of the predictive model. Derivative-free algorithms need nothing further; gradient-based ones are given dC/dθ automatically, obtained by carrying the same reverse pass the GradientMode loop uses one step further, from the forecasts to the parameters.\n\n...\n\nParameters\n\nalgorithm is the Optim.jl algorithm object to use. Defaults to Optim.NelderMead(). Anything Optim provides works, for example Optim.ParticleSwarm(), Optim.SimulatedAnnealing(), Optim.LBFGS(), Optim.BFGS() or Optim.ConjugateGradient().\nparallel is the parallel backend used for the per-sample cost and gradient evaluations. Defaults to SerialBackend(); pass MPIBackend() to distribute them across MPI processes, or DistributedBackend() across Distributed worker processes. See AbstractParallelBackend.\nAny other parameter accepted by Optim.Options, such as iterations, time_limit, show_trace or g_abstol. ...\n\nExample\n\noptions =\n    Options(OptimMode; algorithm = Optim.ParticleSwarm(), iterations = 500)\n\n\n\n\n\n","title":"ApplicationDrivenLearning.OptimMode"},{"category":"type","location":"reference/#ApplicationDrivenLearning.NLoptMode","page":"API Reference","text":"NLoptMode <: AbstractOptimizationMode\n\nUsed to solve the application driven learning training problem with an algorithm from the NLopt.jl package.\n\nnote: Note\nProvided by a package extension: using NLopt is required before training with this mode. The mode type itself always exists, so that a missing using produces a readable error rather than an UndefVarError.\n\n...\n\nParameters\n\nalgorithm::Symbol is the NLopt algorithm to use. Defaults to :LN_NELDERMEAD. The derivative-free families are :LN_* (local, e.g. :LN_BOBYQA, :LN_COBYLA) and :GN_* (global, e.g. :GN_DIRECT); the :LD_* family is gradient-based and is given dC/dθ automatically.\nAny other parameter is set on the NLopt Opt object, so the stopping criteria are named as NLopt names them: xtol_rel, xtol_abs, ftol_rel, ftol_abs, maxeval, maxtime, stopval. Note this differs from OptimMode, where such keywords go to Optim.Options instead.\nparallel is the parallel backend used for the per-sample cost and gradient evaluations. Defaults to SerialBackend(); pass MPIBackend() to distribute them across MPI processes, or DistributedBackend() across Distributed worker processes. See AbstractParallelBackend.\n\n...\n\nExample\n\nusing NLopt\n\noptions =\n    Options(NLoptMode; algorithm = :LN_BOBYQA, xtol_rel = 1e-6, maxeval = 500)\n\n\n\n\n\n","title":"ApplicationDrivenLearning.NLoptMode"},{"category":"type","location":"reference/#ApplicationDrivenLearning.GradientMode","page":"API Reference","text":"GradientMode <: AbstractOptimizationMode\n\nUsed to solve the application driven learning training problem using the gradient optimization method\n\n...\n\nParameters\n\nrule is the Flux/Optimisers rule to be used in the gradient optimization process. Defaults to Flux.Descent().\nepochs is the number of epochs to be used in the gradient optimization process. Defaults to 100.\nbatch_size is the batch size to be used in the gradient optimization process. Defaults to -1, meaning the full dataset is used at every epoch. When positive, each epoch draws batch_size sample indexes uniformly with replacement, so a batch may repeat samples and an epoch does not sweep the whole dataset.\nverbose is the flag of whether to print the training process. Defaults to true.\ncompute_cost_every is the epoch frequency for computing the cost and evaluating best solution. Defaults to 1.\ntime_limit is the time limit for the training process, in seconds. Defaults to Inf.\ng_tol is the tolerance on the infinity norm of the cost gradients with respect to the forecasts, below which training stops. Defaults to 0, which disables the check.\nparallel is the parallel backend used for the per-sample cost and gradient evaluations. Defaults to SerialBackend(); pass MPIBackend() to distribute them across MPI processes, or DistributedBackend() across Distributed worker processes. See AbstractParallelBackend. ...\n\n\n\n\n\n","title":"ApplicationDrivenLearning.GradientMode"},{"category":"type","location":"reference/#ApplicationDrivenLearning.BilevelMode","page":"API Reference","text":"BilevelMode <: AbstractOptimizationMode\n\nUsed to solve the application driven learning training problem as a bilevel optimization problem by using the BilevelJuMP.jl package.\n\n...\n\nParameters\n\noptimizer::Function is equivalent to solver in BilevelJuMP.BilevelModel.\nsilent::Bool is equivalent to silent in BilevelJuMP.BilevelModel.\nmode::Union{Nothing, BilevelJuMP.BilevelMode} is equivalent to mode in BilevelJuMP.BilevelModel. ...\n\n\n\n\n\n","title":"ApplicationDrivenLearning.BilevelMode"},{"category":"type","location":"reference/#ApplicationDrivenLearning.NelderMeadMode","page":"API Reference","text":"NelderMeadMode <: AbstractOptimizationMode\n\nUsed to solve the application driven learning training problem using the Nelder-Mead optimization method implementation from Optim.jl package.\n\ncompat: Deprecated\nSuperseded by OptimMode, which reaches every Optim.jl algorithm rather than just this one. NelderMeadMode behaves exactly as before and is kept so that existing code keeps working; new code should preferOptions(OptimMode; algorithm = Optim.NelderMead())Note that initial_simplex and parameters are Nelder-Mead specific, so under OptimMode they move into the algorithm object: Optim.NelderMead(initial_simplex = ...).\n\n...\n\nParameters\n\ninitial_simplex is the initial simplex of solutions to be applied.\nparameters is the parameters to be applied to the Nelder-Mead optimization method. ...\n\n\n\n\n\n","title":"ApplicationDrivenLearning.NelderMeadMode"},{"category":"type","location":"reference/#ApplicationDrivenLearning.NelderMeadMPIMode","page":"API Reference","text":"NelderMeadMPIMode <: AbstractOptimizationMode\n\nMPI implementation of NelderMeadMode, which distributes the per-sample cost evaluations across MPI processes.\n\ncompat: Deprecated\nParallelism is now an option rather than a mode, so any optimizer can be run over MPI rather than just Nelder-Mead:Options(OptimMode; algorithm = Optim.NelderMead(), parallel = MPIBackend())This mode keeps working and forwards to exactly that, translating mpi_finalize onto the backend.\n\n...\n\nParameters\n\nmpi_finalize::Bool controls whether MPI.Finalize() is called at the end of training. Defaults to true.\nAny other parameter accepted by Optim.Options. ...\n\n\n\n\n\n","title":"ApplicationDrivenLearning.NelderMeadMPIMode"},{"category":"type","location":"reference/#ApplicationDrivenLearning.GradientMPIMode","page":"API Reference","text":"GradientMPIMode <: AbstractOptimizationMode\n\nMPI implementation of GradientMode, which distributes the per-sample cost and gradient evaluations across MPI processes.\n\ncompat: Deprecated\nParallelism is now an option rather than a mode:Options(GradientMode; parallel = MPIBackend())This mode keeps working and forwards to exactly that, translating mpi_finalize onto the backend.\n\n...\n\nParameters\n\nThe same parameters as GradientMode.\nmpi_finalize::Bool controls whether MPI.Finalize() is called at the end of training. Defaults to true. ...\n\n\n\n\n\n","title":"ApplicationDrivenLearning.GradientMPIMode"},{"category":"type","location":"reference/#ApplicationDrivenLearning.AbstractParallelBackend","page":"API Reference","text":"AbstractParallelBackend\n\nHow the per-sample work of a training run is distributed.\n\nEvery setting of a backend lives on the backend object. The one exception is mpi_finalize, which predates this design and is reachable only through the deprecated *MPIMode aliases.\n\nBilevelMode is deliberately outside this: it builds a single MIP over all samples and never evaluates a per-sample cost, so it has nothing to distribute.\n\nImplementations: SerialBackend, MPIBackend, DistributedBackend.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.AbstractParallelBackend"},{"category":"type","location":"reference/#ApplicationDrivenLearning.SerialBackend","page":"API Reference","text":"SerialBackend <: AbstractParallelBackend\n\nEvaluate everything in the current process. The default.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.SerialBackend"},{"category":"type","location":"reference/#ApplicationDrivenLearning.MPIBackend","page":"API Reference","text":"MPIBackend(; finalize = true) <: AbstractParallelBackend\n\nDistribute the per-sample cost and gradient evaluations across MPI processes with JobQueueMPI.jl.\n\nEvery rank runs the same script and so builds its own copy of the model; the controller then drives the optimizer while the workers serve evaluations. Only the controller returns a meaningful Solution.\n\nfinalize controls whether MPI.Finalize() is called when training ends. Set it to false when several train! calls share one MPI session.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.MPIBackend"},{"category":"type","location":"reference/#ApplicationDrivenLearning.DistributedBackend","page":"API Reference","text":"DistributedBackend(; workers = nothing, verify = true) <: AbstractParallelBackend\n\nDistribute the per-sample cost and gradient evaluations across Distributed worker processes.\n\nUnlike MPIBackend, where every rank runs the same script and so builds its own model, here only the driver runs train! — so the workers need a way to obtain a model of their own. That is why this backend must be used with the builder form of train!:\n\nsol = train!(\n    build_case,\n    X,\n    Y,\n    Options(GradientMode; parallel = DistributedBackend()),\n)\n\nPassing a Model instead is an error. The builder is called once per worker per train!, never per sample, and must be defined on the workers (@everywhere, or in a package they load).\n\nIt must also construct the model rather than close over one: an ApplicationDrivenLearning.Model that has had set_optimizer called on it cannot be serialized at all — the attempt is a ReadOnlyMemoryError that takes the process down, since the solver's C pointers travel as raw addresses.\n\nKeyword arguments\n\nworkers: the worker ids to use. Defaults to Distributed.workers() read when training starts, so calling addprocs after building the Options still works.\nverify: check at start-up that the workers' models match the driver's. Their sizes are always compared, which is free; verify additionally compares the assessed cost of one sample, which costs one extra pair of solves on the driver.\nSince the driver and the workers now build from the same function, this guards a narrower thing than it once did: a builder that is not deterministic — one closing over mutable global state, or whose structure varies per process. Cheap enough to leave on.\n\nExample\n\nusing Distributed\naddprocs(4)\n@everywhere using ApplicationDrivenLearning, HiGHS, Flux\n@everywhere function build_case()\n    m = ApplicationDrivenLearning.Model()\n    @variable(m, demand, ApplicationDrivenLearning.Forecast)\n    # ... policy variables, constraints, objectives ...\n    set_optimizer(m, HiGHS.Optimizer)\n    ApplicationDrivenLearning.set_forecast_model(\n        m,\n        ApplicationDrivenLearning.ForecastModel(\n            architecture = Dense(1 => 1),\n            outputs = [demand],\n        ),\n    )\n    return m\nend\n\nsol = train!(\n    build_case,\n    X,\n    Y,\n    Options(GradientMode; parallel = DistributedBackend()),\n)\n\n\n\n\n\n","title":"ApplicationDrivenLearning.DistributedBackend"},{"category":"function","location":"reference/#ApplicationDrivenLearning.plan_policy_vars","page":"API Reference","text":"plan_policy_vars(model::Model)\n\nReturn the vector of Policy variables that belong to the plan model, in declaration order.\n\nThe vector is owned by model and is not a copy; do not mutate it.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.plan_policy_vars"},{"category":"function","location":"reference/#ApplicationDrivenLearning.assess_policy_vars","page":"API Reference","text":"assess_policy_vars(model::Model)\n\nReturn the vector of Policy variables that belong to the assess model, in declaration order (matching plan_policy_vars).\n\nThe vector is owned by model and is not a copy; do not mutate it.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.assess_policy_vars"},{"category":"function","location":"reference/#ApplicationDrivenLearning.plan_forecast_vars","page":"API Reference","text":"plan_forecast_vars(model::Model)\n\nReturn the vector of Forecast variables that belong to the plan model, in declaration order.\n\nThe vector is owned by model and is not a copy; do not mutate it.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.plan_forecast_vars"},{"category":"function","location":"reference/#ApplicationDrivenLearning.assess_forecast_vars","page":"API Reference","text":"assess_forecast_vars(model::Model)\n\nReturn the vector of Forecast variables that belong to the assess model, in declaration order (matching plan_forecast_vars).\n\nThe vector is owned by model and is not a copy; do not mutate it.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.assess_forecast_vars"},{"category":"function","location":"reference/#ApplicationDrivenLearning.set_forecast_model","page":"API Reference","text":"set_forecast_model(model::Model, units; sample_key = nothing)\n\nAttach a forecast to model, described by ForecastModel units.\n\nunits is one ForecastModel or a vector of them. Between them they must predict every Forecast variable declared on model, each exactly once and none belonging to another model — all three are checked here, naming whatever is wrong.\n\nmodel = ApplicationDrivenLearning.Model()\n@variable(model, demand, ApplicationDrivenLearning.Forecast)\n\nset_forecast_model(\n    model,\n    ForecastModel(\n        inputs = [:temp, :hour],\n        architecture = Flux.Dense(2 => 1),\n        outputs = [demand],\n    ),\n)\n\nEverything about the data is declared on the units: writing a unit's inputs as column names is what lets X be matched by name, and writing its outputs as variable => :column names the Y column holding a variable.\n\nsample_key is the one thing that cannot live on a unit, and the only keyword here: it names the column that identifies a row rather than a column, so that realized values are looked up per sample instead of trusted to arrive in the same order as the input.\n\nThe units' Forecast variables are reordered to follow model.forecast_vars, so that the rows of a prediction line up with the forecast parameters of the plan model, and their column names follow them.\n\nReturns the stored FullForecastModel.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.set_forecast_model"},{"category":"function","location":"reference/#ApplicationDrivenLearning.extract_params","page":"API Reference","text":"extract_params(model)\n\nExtract the parameters of a FullForecastModel into a single vector.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.extract_params"},{"category":"function","location":"reference/#ApplicationDrivenLearning.apply_params","page":"API Reference","text":"apply_params(model, θ)\n\nReturn model after fixing the parameters from an adequate vector of parameters.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.apply_params"},{"category":"function","location":"reference/#ApplicationDrivenLearning.apply_gradient!","page":"API Reference","text":"apply_gradient!(model, dCdy, X, opt_state)\n\nApply per-sample cost gradients to the model parameters.\n\nThe optimization layer provides dCdy, the gradient of the assessment cost with respect to the forecasts, for each sample. Because the optimization step itself is not differentiable by the AD backend, these gradients are propagated through the forecast model with a linear surrogate loss whose parameter-gradient equals the chain-rule term (1/T) Σₜ (dC/dŷₜ)·(dŷₜ/dθ).\n\n...\n\nArguments\n\nmodel::FullForecastModel: model to be updated.\ndCdy::AbstractMatrix{<:Real}: per-sample cost gradients, size (T, output_size), with row t aligned to sample t (row t of X).\nX::AbstractMatrix{<:Real}: input data, size (T, input_size).\nopt_state: Optimisers optimisation state. ...\n\n\n\n\n\n","title":"ApplicationDrivenLearning.apply_gradient!"},{"category":"function","location":"reference/#ApplicationDrivenLearning.compute_cost","page":"API Reference","text":"compute_cost(model, X, Y, with_gradients=false)\n\nCompute the cost function (C) based on the model predictions and the true values matrix.\n\n...\n\nArguments\n\nmodel::ApplicationDrivenLearning.Model: model to evaluate.\nX: input data of size (T, input_size). A matrix or vector, whose columns are taken in order, or a Tables.jl-compatible table such as a DataFrame, whose columns are selected by the predictive model's input_names.\nY: realized values. A (T, output_size) matrix, a vector, a Tables.jl-compatible table, or a Dict mapping each Forecast variable to its series. Table columns are matched to the forecast variables by name, never by position; see set_forecast_model for how those names are declared.\nwith_gradients::Bool=false: flag to compute and return the cost gradients with respect to the forecasts. When set, the second returned value is the per-sample gradient matrix of size (T, output_size).\naggregate::Bool=true: when true, the returned cost is averaged over the T samples. Only affects the cost; gradients are always per-sample. ...\n\n\n\n\n\ncompute_cost(model, X, Y, with_gradients=false, aggregate=true)\n\nVariant of compute_cost accepting any combination of the supported input containers — matrices, vectors, Tables.jl-compatible tables such as a DataFrame, and a Dict mapping each Forecast variable to its series. Both arguments are normalized to matrices together — which is what lets the rows be matched by sample_key — and the call is forwarded to the matrix method.\n\nX and Y are @nospecialized. Without it this wrapper is specialized on the concrete container types, and specializing it drags the whole compute_cost body — sample loop, solver calls and all — through inference again in the new context: measured at 174 s for the first table-typed call, against 0.001 s once compiled. Nothing here benefits from specialization, since both arguments are immediately converted to matrices.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.compute_cost"},{"category":"function","location":"reference/#ApplicationDrivenLearning.train!","page":"API Reference","text":"train!(model::Model, X, Y, options::Options)\n\nTrain the predictive model of model so that it minimizes the assessed cost of the application.\n\n...\n\nArguments\n\nmodel::ApplicationDrivenLearning.Model: model to train. Its forecast model must have been set with set_forecast_model.\nX: input data of size (T, input_size). A matrix or vector, whose columns are taken in order, or a Tables.jl-compatible table such as a DataFrame, whose columns are selected by the predictive model's input_names.\nY: realized values of size (T, output_size). A matrix or vector whose columns follow the predictive model output order, a Tables.jl-compatible table whose columns are matched to the Forecast variables by name, or a dictionary mapping each Forecast variable to its length-T series.\n\nA table is always matched by name and never falls back to column order; see set_forecast_model for how those names are declared.\n\noptions::Options: training mode and its parameters.\n\nReturns a Solution with the best cost found and the corresponding parameter vector. The predictive model of model is updated in place with those parameters. ...\n\n\n\n\n\ntrain!(build_model::Function, X, Y, options::Options)\n\nTrain a model built by build_model, a zero-argument function returning a ready-to-solve Model — variables, constraints, objectives, set_optimizer and set_forecast_model all done.\n\nEquivalent to building the model and calling train! on it, except that the function itself is available to the parallel backend. That is what DistributedBackend needs: its workers never run your script, and a model that has had set_optimizer called on it cannot be sent to them at all, so a builder is the only way they can obtain one. Passing a Model under that backend is an error.\n\nUsing one function for the whole run is also what makes the driver's model and the workers' models the same problem by construction, rather than two descriptions that have to be checked against each other.\n\nnote: Note\nThe caller keeps no reference to the model this builds. The fitted parameters are in the returned Solution, so a fitted model is recovered withsol = train!(build_model, X, Y, options)\nm = build_model()\nApplicationDrivenLearning.apply_params(m.forecast, sol.params)Both methods return a Solution; a return type that depended on which one was called would be worse than this.\n\n\n\n\n\n","title":"ApplicationDrivenLearning.train!"},{"category":"section","location":"tutorials/parallel_backends/#Running-in-parallel","page":"Running in parallel","text":"Parallelism is an option, not a mode. The parallel keyword can be given to OptimMode, NLoptMode and GradientMode alike:\n\nopt = ApplicationDrivenLearning.Options(\n    ApplicationDrivenLearning.GradientMode,\n    rule = Flux.Adam(0.1),\n    epochs = 30,\n    parallel = ApplicationDrivenLearning.MPIBackend(),\n)\nsol = ApplicationDrivenLearning.train!(model, X, Y, opt)\n\nThe optimization algorithm is unchanged; what is distributed is the per-sample work, i.e. solving the plan and assess models (and, when gradients are needed, differentiating the plan model) for each row of the dataset. That is the only thing distributed — the optimizer, the epoch loop and the parameter update all run in the driver process — which is why the backend is independent of the mode.\n\nBilevelMode is the exception: it builds a single MIP over all samples and never evaluates a per-sample cost, so there is nothing to distribute and it does not accept parallel.\n\nThis is worthwhile when the plan/assess models are expensive relative to the predictive model, which is the usual case: cost evaluation is O(T) solver calls per iteration and those calls are independent.","title":"Running in parallel"},{"category":"section","location":"tutorials/parallel_backends/#Choosing-a-backend","page":"Running in parallel","text":" MPIBackend DistributedBackend\nLaunching mpiexec -n N julia script.jl addprocs(N), works from the REPL\nGetting a model onto the workers every rank runs the whole script train!(build_case, ...) with a builder function\nReturn value every rank returns; only the controller's is meaningful only the driver runs train! at all\nRequirements a working MPI runtime none; Distributed is a stdlib\nAcross machines the usual HPC route, via the scheduler addprocs with SSH\n\nPrefer DistributedBackend for interactive work and for a single machine — there is nothing to install and no launcher to remember. Prefer MPIBackend on a cluster whose scheduler already speaks MPI.\n\nBoth distribute exactly the same function, so switching between them changes neither the result nor the optimizer. The tests assert this: each backend must reproduce the serial cost and the serial parameters.\n\n","title":"Choosing a backend"},{"category":"section","location":"tutorials/parallel_backends/#Running-over-MPI","page":"Running in parallel","text":"Uses JobQueueMPI.jl. The rank 0 process acts as the controller: it runs the optimization algorithm, holds the incumbent parameters and dispatches one job per sample. All other ranks are workers: they receive a parameter vector and a sample index, apply the parameters to the forecast model, solve the plan and assess models for that sample, and send the cost (and gradient) back.\n\nBecause every rank builds the full ApplicationDrivenLearning.Model, the model definition script must be executed by all ranks — only the train! call behaves differently depending on the rank.","title":"Running over MPI"},{"category":"section","location":"tutorials/parallel_backends/#Running-a-script","page":"Running in parallel","text":"MPI is not usable from an interactive session; the script has to be launched under an MPI runner with more than one process:\n\n$ mpiexec -n 4 julia --project my_training_script.jl\n\nWith -n 4 there is one controller and three workers.","title":"Running a script"},{"category":"section","location":"tutorials/parallel_backends/#Arguments","page":"Running in parallel","text":"Every mode keeps its own arguments unchanged; parallel is the only addition. MPIBackend itself takes one:\n\nfinalize: whether MPI.Finalize() is called at the end of train!. Defaults to true. Set it to false when you want to run several train! calls (or other MPI work) in the same process.\n\n# any optimizer, distributed the same way\nOptions(OptimMode; algorithm = Optim.ParticleSwarm(), parallel = MPIBackend())\nOptions(NLoptMode; algorithm = :LN_BOBYQA, parallel = MPIBackend())\nOptions(GradientMode; rule = Flux.Adam(0.1), parallel = MPIBackend(finalize = false))","title":"Arguments"},{"category":"section","location":"tutorials/parallel_backends/#Return-value","page":"Running in parallel","text":"Only the controller process returns a meaningful Solution; worker processes return a placeholder and their forecast model is left in an unspecified state. Guard any post-processing accordingly.\n\nwarning: Warning\nfinalize defaults to true, and once MPI is finalized no MPI routine may be called again — including JQM.is_controller_process(). So either capture the rank before training:import JobQueueMPI as JQM\n\nJQM.mpi_init()\nis_controller = JQM.is_controller_process()\n\nsol = ApplicationDrivenLearning.train!(model, X, Y, opt)\nif is_controller\n    println(sol.cost)\nendor keep MPI alive until the post-processing is done:opt = ApplicationDrivenLearning.Options(\n    ApplicationDrivenLearning.GradientMode;\n    epochs = 30,\n    parallel = ApplicationDrivenLearning.MPIBackend(finalize = false),\n)\nsol = ApplicationDrivenLearning.train!(model, X, Y, opt)\nif JQM.is_controller_process()\n    println(sol.cost)\nend\nJQM.mpi_finalize()\n\nThe second form is also what you need when running several train! calls in the same process: every call but the last must pass finalize = false.\n\ncompat: Deprecated: NelderMeadMPIMode and GradientMPIMode\nThese were the only way to run over MPI before parallelism became an option, and only Nelder-Mead had one. They still work, forwarding to parallel = MPIBackend() and translating mpi_finalize onto the backend:# before                                    # now\nOptions(NelderMeadMPIMode; iterations = 100)\nOptions(OptimMode; algorithm = Optim.NelderMead(), iterations = 100,\n        parallel = MPIBackend())\n\nOptions(GradientMPIMode; epochs = 30)\nOptions(GradientMode; epochs = 30, parallel = MPIBackend())\n\n","title":"Return value"},{"category":"section","location":"tutorials/parallel_backends/#Running-over-Distributed","page":"Running in parallel","text":"Uses Julia's own Distributed stdlib, so there is nothing to install and no external launcher. Unlike MPI, only the driver process runs train!; the workers never execute your script.\n\nThat is the one difference that shows up in the API. The workers still need a model of their own to solve against, and since they never ran your script they have no way to obtain one — so instead of handing train! a model, you hand it a function that builds one:\n\nusing Distributed\naddprocs(4)\n@everywhere using ApplicationDrivenLearning, Flux, JuMP, HiGHS\n\n# `@everywhere` so that the workers can call it too\n@everywhere function build_case()\n    m = ApplicationDrivenLearning.Model()\n    @variable(m, demand, ApplicationDrivenLearning.Forecast)\n    # ... policy variables, constraints and objectives ...\n    set_optimizer(m, HiGHS.Optimizer)\n    ApplicationDrivenLearning.set_forecast_model(\n        m,\n        ApplicationDrivenLearning.ForecastModel(\n            architecture = Chain(Dense(1 => 1)),\n            outputs = [demand],\n        ),\n    )\n    return m\nend\n\nopt = ApplicationDrivenLearning.Options(\n    ApplicationDrivenLearning.GradientMode,\n    rule = Flux.Adam(0.1),\n    epochs = 30,\n    parallel = ApplicationDrivenLearning.DistributedBackend(),\n)\nsol = ApplicationDrivenLearning.train!(build_case, X, Y, opt)\n\ntrain! calls it once for the driver's own model and each worker calls it once for its own — so a single function describes the whole run, and the driver and the workers cannot end up solving different problems. Passing a Model under this backend is an error, since there would be no way to give the workers one.\n\nThe builder returns a ready-to-solve model: variables, constraints, objectives, set_optimizer and set_forecast_model. It is called once per worker per train!, never per sample, and must be visible on the workers (@everywhere, or defined in a package they load).\n\nThe workers' initial predictive-model weights do not matter, so a random init is fine: every evaluation applies parameters sent from the driver before solving.\n\nThis is the same function an MPI script already needs in order to have every rank build an identical case, so an existing MPI setup can usually pass its builder unchanged.\n\nwarning: Warning\nThe builder must construct the model rather than close over one. An ApplicationDrivenLearning.Model that has had set_optimizer called on it cannot be sent to a worker at all: the solver's internal pointers travel as raw addresses and the attempt is a ReadOnlyMemoryError, which takes the process down rather than raising a catchable error. Building inside the builder avoids this entirely.","title":"Running over Distributed"},{"category":"section","location":"tutorials/parallel_backends/#Getting-the-trained-model-back","page":"Running in parallel","text":"The builder form leaves you without a reference to the model that was trained. The fitted parameters are in the returned Solution, so a fitted model is one call away:\n\nsol = ApplicationDrivenLearning.train!(build_case, X, Y, opt)\n\nmodel = build_case()\nApplicationDrivenLearning.apply_params(model.forecast, sol.params)","title":"Getting the trained model back"},{"category":"section","location":"tutorials/parallel_backends/#Arguments-2","page":"Running in parallel","text":"workers: which worker ids to use. Defaults to Distributed.workers(), read when training starts — so calling addprocs after building the Options still works. If there are no worker processes, training runs on the driver at serial speed and warns, since the usual cause is a forgotten addprocs.\nverify: whether to check at start-up that the workers' models match the driver's. Defaults to true; see below.\n\nDistributedBackend()\nDistributedBackend(; workers = [2, 3])\nDistributedBackend(; verify = false)","title":"Arguments"},{"category":"section","location":"tutorials/parallel_backends/#The-workers'-models-are-checked-against-the-driver's","page":"Running in parallel","text":"Since one function builds them all, the only way they can disagree is if that function is not deterministic — if it closes over mutable global state, or branches on something that differs per process. Narrow, but silent: the run would simply optimize the wrong problem. So DistributedBackend checks, once per train!:\n\nthe models' sizes always: input and output size, the number of policy and forecast variables, and the number of predictive-model parameters;\nthe assessed cost of one sample at the starting parameters, when verify is true.\n\nThe cost comparison is the one that catches a difference the sizes cannot see, which is why it is on by default. It is a smoke test rather than a proof — one sample cannot reveal a difference that only shows on another — and it costs one extra pair of solves on the driver. Pass verify = false to skip it.\n\nIf the driver's model has no optimizer attached, the cost comparison cannot run; the sizes are still checked and a warning says the rest was skipped.","title":"The workers' models are checked against the driver's"},{"category":"section","location":"tutorials/parallel_backends/#Return-value-2","page":"Running in parallel","text":"Only the driver runs train!, so there is no rank-dependent behaviour and no placeholder to guard against: sol is the real Solution.\n\nThe workers release their models when train! returns, including when it throws, so a later train! with a different model never inherits a stale one.","title":"Return value"},{"category":"section","location":"tutorials/parallel_backends/#Cost-of-the-first-run","page":"Running in parallel","text":"The first distributed evaluation makes each worker compile the JuMP, DiffOpt and solver code paths for itself, which takes appreciably longer than the training that follows. This is per worker process, not per train!, so it is paid once per session and is worth keeping in mind when timing a short run.","title":"Cost of the first run"},{"category":"section","location":"#ApplicationDrivenLearning.jl-Documentation","page":"Home","text":"","title":"ApplicationDrivenLearning.jl Documentation"},{"category":"section","location":"#Introduction","page":"Home","text":"ApplicationDrivenLearning.jl is a Julia package for training time series forecast models using the application driven learning framework, that connects the optimization problem final cost with predictive model parameters in order to achieve the best model for a given application.","title":"Introduction"},{"category":"section","location":"#Installation","page":"Home","text":"Install ApplicationDrivenLearning with Julia's built-in package manager:\n\njulia> import Pkg\n\njulia> Pkg.add(\"ApplicationDrivenLearning\")\n\nFor properly modelling plan, assess and predictive models, you will also need JuMP and Flux packages.\n\njulia> Pkg.add(\"JuMP\")\njulia> Pkg.add(\"Flux\")\n\nFinally, for actually training the model, you will need to be able to solve the optimization models and the appropriate package depending on the training mode.","title":"Installation"},{"category":"section","location":"#Getting-started","page":"Home","text":"Learn the basics of JuMP and Julia in the JuMP documentation\nFollow the tutorials in this manual\n\nIf you need help, please open a GitHub issue.","title":"Getting started"},{"category":"section","location":"#License","page":"Home","text":"ApplicationDrivenLearning.jl is licensed under the MIT License.","title":"License"},{"category":"section","location":"#Contributing","page":"Home","text":"PRs such as adding new training modes and fixing bugs are very welcome!\nFor nontrivial changes, you'll probably want to first discuss the changes via issue.","title":"Contributing"}]
}
